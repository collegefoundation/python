# Check if a string is non-empty and if so verify if it contains more than 10 characters
# input a string form user
string = (str)(input("Enter a string: "))
#check condition for non-empty
if(len(string) != 0):    
    print("---------------------------------")
    print("Enter String: ",string)
    #Length Count
    length=len(string)
    if(length > 10):
       print("String is  contains more than 10 characters.")
    else:
         print("String is not contains more than 10 characters.")     

else:
    print("String is Empty.")
